# GitHub Actions workflow for releasing binaries
# Generated by gpy (go-package-yourself)
# DO NOT EDIT - regenerate with: gpy workflow --write

name: Release
"on":
    push:
        tags:
            - v*
    workflow_call: {}
jobs:
    build:
        runs-on: ${{ matrix.runs-on }}
        strategy:
            matrix:
                include:
                    - os: darwin
                      arch: amd64
                      runs-on: macos-latest
                      archive: go-package-yourself__darwin_amd64.tar.gz
                      bin-path: go-package-yourself
                      ext: .tar.gz
                    - os: darwin
                      arch: arm64
                      runs-on: macos-latest
                      archive: go-package-yourself__darwin_arm64.tar.gz
                      bin-path: go-package-yourself
                      ext: .tar.gz
                    - os: linux
                      arch: amd64
                      runs-on: ubuntu-latest
                      archive: go-package-yourself__linux_amd64.tar.gz
                      bin-path: go-package-yourself
                      ext: .tar.gz
                    - os: linux
                      arch: arm64
                      runs-on: ubuntu-latest
                      archive: go-package-yourself__linux_arm64.tar.gz
                      bin-path: go-package-yourself
                      ext: .tar.gz
                    - os: windows
                      arch: amd64
                      runs-on: windows-latest
                      archive: go-package-yourself__windows_amd64.zip
                      bin-path: go-package-yourself.exe
                      ext: .zip
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                fetch-depth: 0
            - name: Setup Go
              uses: actions/setup-go@v6
              with:
                go-version: "1.22"
            - name: Build binary
              env:
                GOARCH: ${{ matrix.arch }}
                GOOS: ${{ matrix.os }}
              run: go build -o ${{ matrix.bin-path }} -ldflags "-X main.Version=${{ github.ref_name }}" ./cmd/gpy
            - name: Create archive (Unix)
              if: ${{ matrix.os != 'windows' }}
              run: tar czf "${{ matrix.archive }}" "${{ matrix.bin-path }}"
              shell: bash
            - name: Create archive (Windows)
              if: ${{ matrix.os == 'windows' }}
              run: Compress-Archive -Path "${{ matrix.bin-path }}" -DestinationPath "${{ matrix.archive }}" -Force
              shell: pwsh
            - name: Generate checksums (Unix)
              if: ${{ matrix.os != 'windows' }}
              run: |-
                sha256sum "${{ matrix.archive }}" | sed 's/  .*\//  /' >> checksums.txt
                echo "${{ matrix.archive }}" >> release-files.txt
                echo checksums.txt >> release-files.txt
              shell: bash
            - name: Generate checksums (Windows)
              if: ${{ matrix.os == 'windows' }}
              run: |-
                $hash = (Get-FileHash -Path '${{ matrix.archive }}' -Algorithm SHA256).Hash
                Add-Content -Path checksums.txt -Value "$hash  ${{ matrix.archive }}"
                Add-Content -Path release-files.txt -Value '${{ matrix.archive }}'
                Add-Content -Path release-files.txt -Value 'checksums.txt'
              shell: pwsh
            - name: Upload artifacts
              uses: actions/upload-artifact@v4
              with:
                name: release-${{ matrix.os }}-${{ matrix.arch }}
                path: |-
                    release-files.txt
                    ${{ matrix.archive }}
                    checksums.txt
    publish-chocolatey:
        needs:
            - build
        runs-on: windows-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Setup Go
              uses: actions/setup-go@v6
              with:
                go-version: "1.22"
            - name: Pack Chocolatey package
              run: |-
                # Navigate to committed generated package and pack it
                cd "packaging\chocolatey\go-package-yourself"
                choco pack
              shell: pwsh
            - name: Push to Chocolatey
              env:
                ChocolateyApiKey: ${{ secrets.CHOCOLATEY_API_KEY }}
              if: env.ChocolateyApiKey != ''
              run: |-
                # Push the generated .nupkg to Chocolatey.org
                cd "packaging\chocolatey\go-package-yourself"
                $nupkg = Get-ChildItem -Filter "*.nupkg" | Select-Object -First 1
                choco push "$($nupkg.FullName)" --source=https://push.chocolatey.org/
              shell: pwsh
    publish-homebrew:
        needs:
            - build
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Setup Go
              uses: actions/setup-go@v6
              with:
                go-version: "1.22"
            - name: Publish Homebrew formula
              env:
                GH_TOKEN: ${{ secrets.HOMEBREW_CORE_PAT }}
              if: env.GH_TOKEN != ''
              run: |-
                #!/bin/bash
                set -e

                # Configure git
                git config --global user.name "gpy-bot"
                git config --global user.email "gpy@github.local"

                # Clone/fork homebrew-core (using GitHub CLI)
                BRANCH_NAME="gpy-$(git rev-parse --short HEAD)"
                gh repo fork homebrew/homebrew-core --clone --branch "$BRANCH_NAME"

                # Copy committed generated formula to the fork
                cd homebrew-core
                FORMULA_FILE="../packaging/homebrew/GoPackageYourself.rb"
                FORMULA_NAME=$(basename "$FORMULA_FILE")
                cp "$FORMULA_FILE" "Formula/$FORMULA_NAME"

                # Commit and push
                git add "Formula/$FORMULA_NAME"
                git commit -m "feat: add gpy-generated formula for $(basename $FORMULA_FILE .rb)"
                git push origin "$BRANCH_NAME"

                # Create PR
                gh pr create \
                  --repo homebrew/homebrew-core \
                  --title "feat: add formula for $(basename $FORMULA_FILE .rb)" \
                  --body "Generated by [gpy](https://github.com/users/gpy-bot/repos)" \
                	  --head "$(git config --global user.name):$BRANCH_NAME"
    publish-npm:
        needs:
            - build
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                node-version: "24"
                registry-url: https://registry.npmjs.org/
            - name: Setup Go
              uses: actions/setup-go@v6
              with:
                go-version: "1.22"
            - name: Publish npm package
              env:
                NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
              if: env.NODE_AUTH_TOKEN != ''
              run: |-
                # Publish the committed gpy-generated npm package
                cd "packaging/npm/go-package-yourself"
                npm publish
    release:
        needs:
            - build
        permissions:
            contents: write
        runs-on: ubuntu-latest
        steps:
            - name: Download artifacts
              uses: actions/download-artifact@v4
              with:
                merge-multiple: false
                path: release-artifacts
                pattern: release-*
            - name: List artifacts
              run: find release-artifacts -type f -exec ls -lh {} \;
            - name: Upload to GitHub Release
              uses: softprops/action-gh-release@v3
              with:
                files: release-artifacts/**/*
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
